server {
	server_name _;
	index index.html;
	error_log /var/log/nginx/error.log;
	access_log /var/log/nginx/access.log;
	root /client;

	location / {
		try_files $uri $uri/ @server-client;
	}

	location @server-client {
		 proxy_pass http://server-client;
	}
}
