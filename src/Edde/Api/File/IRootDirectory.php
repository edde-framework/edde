<?php
	declare(strict_types = 1);

	namespace Edde\Api\File;

	/**
	 * Marker interface for a special case of a root directory (usually of an application).
	 */
	interface IRootDirectory extends IDirectory {
	}
