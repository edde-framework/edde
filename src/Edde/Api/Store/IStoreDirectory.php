<?php
	declare(strict_types=1);

	namespace Edde\Api\Store;

	use Edde\Api\File\IDirectory;

	/**
	 * Where store would live on filesystem (if needed).
	 */
	interface IStoreDirectory extends IDirectory {
	}
