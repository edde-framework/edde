<?php
	declare(strict_types = 1);

	namespace Edde\Api;

	use Edde\Api\File\IDirectory;

	/**
	 * Formal interface defining where the framework lives (for asset reference).
	 */
	interface IEddeDirectory extends IDirectory {
	}
